%h1 Listing user_meeting_rooms

%table
  %tr
    %th
    %th
    %th

  - @user_meeting_rooms.each do |user_meeting_room|
    %tr
      %td= link_to 'Show', user_meeting_room
      %td= link_to 'Edit', edit_user_meeting_room_path(user_meeting_room)
      %td= link_to 'Destroy', user_meeting_room, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New User meeting room', new_user_meeting_room_path
