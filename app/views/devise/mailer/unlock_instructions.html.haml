%.align_centered 
  ='Hello'
  = @resource.email
  ='!'

%p.align_centerd Your account has been locked due to an excessive amount of unsuccessful sign in attempts.

%p.align_centered Click the link below to unlock your account:

%p.align_centered
  = link_to 'Unlock my account', unlock_url(@resource, :unlock_token => @resource.unlock_token) 
