%p.align_centered
  = 'Hello'
  = @resource.email
  ='!'

%p.align_centered Someone has requested a link to change your password, and you can do this through the link below.

%p.align_centered
  = link_to 'Change my password', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token)

%p.align_centered If you didn't request this, please ignore this email.
%p.align_Centered Your password won't change until you access the link above and create a new one.
